<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth: discovery + connection. No INTERNET permission on purpose:
         SignalBridge moves voice over local radio only, never over mobile data. -->

    <!-- Android 12+ (API 31+) runtime Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Android 10-11 (API 29-30) legacy Bluetooth perms + FINE_LOCATION, which those
         versions REQUIRE for BLE scanning to return any results. Capped so newer
         versions use the runtime perms above instead. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <!-- Capped at 32: needed for BLE scan (<31) and Wi-Fi Direct discovery (<33). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="32" />

    <!-- Wi-Fi Direct long-range mode -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES"
        android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- INTERNET here is only Android's name for "may open a socket". SignalBridge uses it
         solely for the LOCAL Wi-Fi Direct group (phone-to-phone). No remote hosts, no
         mobile data — there is no server anywhere in this app. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Microphone for voice capture -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Foreground service so a live voice link survives the screen turning off -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />

    <!-- EXPERIMENTAL cellular-bridge module only (see bridge/CellularBridge.kt).
         Declared but the feature is opt-in and off by default. -->
    <uses-permission android:name="android.permission.MANAGE_OWN_CALLS" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.wifi.direct" android:required="false" />

    <application
        android:allowBackup="false"
        android:icon="@android:drawable/sym_action_call"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SignalBridge">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".session.VoiceLinkService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
</manifest>
